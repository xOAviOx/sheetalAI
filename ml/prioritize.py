"""Equity-weighted prioritisation — Phase 5.

Implemented in its phase. Scaffold only.
"""

from __future__ import annotations

from config import load_city


def main() -> None:
    cfg = load_city()
    raise SystemExit(f"[prioritize] not yet implemented (city={cfg.key}). Scaffold only.")


if __name__ == "__main__":
    main()
